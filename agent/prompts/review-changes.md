---
description: Review the current branch and local changes using codebase research plus a dedicated React UI/usability review
chain: scout -> ui-reviewer -> reviewer
---

This prompt is kept for `/prompt-workflow review-changes`, but the more reliable command for the full scout + UI reviewer + final reviewer flow is:

/run-chain review-changes
